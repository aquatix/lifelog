# Write json data files


def createproject(destinationdir):
    """
    Writes generated project files into `destinationdir`, ready to process with Pelican

    :param str destinationdir: directory where to write the data to
    """
    print(f"Writing json data files to {destinationdir}")
    return
